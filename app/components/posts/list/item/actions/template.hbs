{{#if this.isAlreadyLikedByCurrentUser}}
  <button
    type='button'
    class='card-link btn btn-link'
    {{on 'click' this.onDislike}}
  >
    Dislike ({{@post.likes.length}})
    <Posts::List::Item::Actions::WhoLikesTooltip @post={{@post}} />
  </button>
{{else}}
  <button
    type='button'
    class='card-link btn btn-link'
    {{on 'click' this.onLike}}
  >
    Like ({{@post.likes.length}})
    <Posts::List::Item::Actions::WhoLikesTooltip @post={{@post}} />
  </button>
{{/if}}

{{#if this.isCreatedByCurrentUser}}
  <button
    type='button'
    class='card-link btn btn-link'
    {{on 'click' this.onDelete}}
  >Delete</button>{{/if}}